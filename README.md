# stereopipe
it's a secret

```mermaid
graph TD;
  A[Data preprocessing]--> B[Variant calling];
  B--> C[Neoantigen prediction];
  B--> D[Variant processing];
  C--> D;
  D--> E[Downstream analyses];
```
